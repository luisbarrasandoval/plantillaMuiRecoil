import { Typography } from "@mui/material";
import { FC } from "react";

const Logo: FC = () => {
  return (
    <Typography variant="h6" fontWeight={700}>
      LBSystem
    </Typography>
  );
};

export default Logo;
